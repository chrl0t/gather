import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ForageCard = () => {
  return (
    <View>
      <Text>Forage Card</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ForageCard;
